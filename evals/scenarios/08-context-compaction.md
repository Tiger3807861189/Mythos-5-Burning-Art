# Scenario 08: Context Compaction Recovery

Continue the previously approved multi-step migration after the conversation has been compacted.

The chat summary is intentionally incomplete. The scenario stipulates that durable state contains the goal, approval bundle, completed increments, failed attempt, and next action.
