<!-- MYTHOS-5-BURNING-ART:BEGIN -->
@AGENTS.md

# Claude Code Host Binding

The import above is the shared Mythos 5 Burning Art repository contract. The activation kit must install or merge AGENTS.md at the repository root before enabling this file. If the import is absent or fails, disclose the missing contract and do not claim full Mythos enforcement.

For every substantive task, invoke mythos-5-burning-art:mythos-orchestrate before implementation. Seal `MYTHOS_REVIEW_PACKET_V1` before launching the dedicated named mythos-plan-critic through Agent with the exact neutral prompt `Review only the hook-injected sealed packet. Do not use or request parent-session reasoning, a desired verdict, or an artifact summary.`. Never use `fork`. After implementation, seal `MYTHOS_VERIFICATION_PACKET_V1` and launch a different new named mythos-verifier the same way. Require each reviewer to echo its hook-owned hash and never ask either reviewer to spawn another agent.

Claude plugin agents load repository CLAUDE.md and rules into their separate named-agent context. Include those sources in the context manifest. Fresh review requires the hook-bound neutral Agent request, the dedicated named type rather than fork, a new agent ID, and a distinct nested subagent transcript; repository governance still applies.

Plugin hooks work only while the plugin is enabled and policy permits them. A missing Python 3.11 executable, failed hook, unsupported event, or unobserved tool is a degraded state. Stop before a protected mutation and request human intervention rather than pretending the gate is enforced.
<!-- MYTHOS-5-BURNING-ART:END -->


